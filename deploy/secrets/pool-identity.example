apiVersion: v1
kind: Secret
metadata:
  name: pool-identity
  namespace: apps
type: Opaque
data:
  # Plantilla: los valores reales se inyectan desde el GitHub Secret
  pool-identity.key: REPLACE_WITH_BASE64_PRIVATE_KEY
  pool-identity.key.pub: REPLACE_WITH_BASE64_PUBLIC_KEY